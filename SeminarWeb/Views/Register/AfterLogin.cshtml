
@{
    ViewBag.Title = "AfterLogin";
}

<h2>AfterLogin</h2>

@if (Session["ID"] != null)
{
    <div>
        Welcome : <a href="#">@Session["UserID"]</a><br />
        <a href="@Url.Action("Logout", "Register")">Logout</a>
    </div>
}

